#!/bin/bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"

cd "$PROJECT_ROOT"
export DISPLAY=:0

# Call the muscle_personalizer binary directly using current shell environment
exec build/release/viewer/muscle_personalizer "$@" 